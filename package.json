{
  "name": "min-approvals-action",
  "module": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "bun build ./src/index.ts --outdir ./dist --outfile ./dist/index.js --target=node --sourcemap=linked --minify",
    "lint": "tsc --noEmit && biome check",
    "lint:fix": "biome check --fix"
  },
  "devDependencies": {
    "@biomejs/biome": "2.3.13",
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@actions/core": "^3.0.0",
    "@actions/github": "^9.0.0",
    "@octokit/openapi-types": "^27.0.0"
  }
}
