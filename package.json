{
  "name": "min-approvals-action",
  "module": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "bun build ./src/index.ts --outdir ./dist --outfile ./dist/index.js --target=node --sourcemap=linked --minify",
    "lint": "tsc --noEmit && biome check",
    "lint:fix": "biome check --fix"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.11",
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@actions/core": "^3.0.1",
    "@actions/github": "^9.1.1",
    "@octokit/openapi-types": "^29.0.0"
  }
}
